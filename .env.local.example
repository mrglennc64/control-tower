# Where the dashboard stores its JSON data files.
# Defaults to the OneDrive mailman/data folder if unset (see lib/store.ts).
# Copy this file to .env.local and adjust if your OneDrive path differs.
DATA_DIR=C:\Users\carin\OneDrive\Dokument\mailman\data
